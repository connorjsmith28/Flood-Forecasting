# DuckDB database files (global pattern for stray DBs)
*.duckdb
*.duckdb.wal
!data/database/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Dagster
.dagster/
storage/
.tmp_dagster_home*/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Environment variables
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Data files (large datasets)
*.parquet
*.csv
!**/seeds/*.csv
!data/gage_connections.csv

# DuckDB database files (generated, not tracked)
data/database/*.duckdb
data/database/*.duckdb.wal

# Preprocessed data files (stored in W&B artifacts, not git)
*.npy
artifacts/
pipeline_outputs/

# HTTP cache (aiohttp-client-cache)
cache/
.cache/

# Secrets
secrets

# Weights & Biases
# These are local log files, they are already synced to cloud so unless you run in offline mode we don't need to track them.
wandb/
